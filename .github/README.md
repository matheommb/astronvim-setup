# Personal astro-nvim setup

Modify in plugins/user.lua for installing plugins or create a lua file in plugins with the name of the plugin with all the onfiguration needed.

Mappings.lua can also be modified to match the keybindings wanted.

Finally, options can also be modified to match those wanted such as relative number, etc. 
